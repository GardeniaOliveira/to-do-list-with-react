const Input = ({ name, type, placeholder }) => {
    return (
        <input
            type={type}
            name={name}
            placeholder={placeholder}

        />
    )
}

export default Input; 